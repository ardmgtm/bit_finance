import 'package:flutter/material.dart';

class StatiscticScreen extends StatelessWidget {
  const StatiscticScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        AppBar(
          title: const Text('Statistic'),
          elevation: 0,
        ),
      ],
    );
  }
}
